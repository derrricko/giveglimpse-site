# TODOS — giveglimpse.com

Format: what · why · context · effort (human → CC) · priority · depends on.

## P1
- **Per-need atom inside the event record.** The briefs define the record per *event*; the old site's identity was one need, four words, one X thread a stranger can check in ten seconds. Design the per-need row (posted → funded → bought → proven, evidence link, X thread link, no name, no plate) inside `event-01.html#event-record` so the October 31 entry has a shape before the day. Effort M → S. Depends on: nothing; do before Oct 31.
- **Real photograph of the bay** on the home "first test" block and Event 01, replacing the 2016 stock shot. No people. Effort S → S. Depends on: the camera visit to the dealership.
- **Name the dealership** in the Event 01 facts list, with a one-line quote in the owner's words. Effort S → S. Depends on: the owner's permission.
- **Flip the hero object by hand** in `script.js` (`EVENT`): `startsAt` once the stream time is real (hours show on the last day); `live: true` only while on air (LIVE + link, never inferred); `record: true` after the event (countdown gone, the card is the record; also automatic once the date has passed), then edit the five card rows and link the receipts. Effort S → S. Depends on: each moment arriving.

## P2
- ~~Home mechanism strip + "the honest part."~~ Done 2026-09-25 on branch `next-window`: three-sentence strip under the status bar, honest-part block after the money tiles.

## Not scheduled (2026-09-25)
- Labeled example entry on Event 01 ("example, not a record").
- Shared head/footer template across the seven pages; Safari and 320px device pass of the opening sequence.
